# 标签覆盖缺口 / Tag Coverage Gaps

基于 `src/lib/tags.ts` 中 63 个方法标签，对现有 139 道题目（`src/data/problems/*.ts`）与 39 个知识点（`src/data/knowledge/*.ts`）做覆盖率审计，产出需要补写题目或知识点的清单。

- **统计口径**：每道题的 `tags` 字段是"解题方法/思维方法"；知识点的 `tag` 字段是它对应的方法标签。
- **阈值约定**：知识点每个标签应至少 1 篇讲解；题目每个标签建议至少 3 道（便于"使用该方法的例题"列表有梯度）。

---

## 一、知识点缺口（已补齐 12 / 12，✅ 表示已补）

这些标签在 `src/lib/tags.ts` 里已声明，但 `src/data/knowledge/*.ts` 里没有对应的 `tag:` 条目，访问 `/k/:slug` 没有页面可跳转。

| 标签              | 类别        | 建议所在文件                | 备注                                                                                             |
| :---------------- | :---------- | :-------------------------- | :----------------------------------------------------------------------------------------------- | --- |
| ✅ 加法原理       | counting    | `knowledge/counting.ts`     | 已新增 `addition-principle` 页，作为“加乘原理”拆分中的“分类→加法”                                |
| ✅ 乘法原理       | counting    | `knowledge/counting.ts`     | 已新增 `multiplication-principle` 页；与 `addition-principle` 成对链回 `additive-multiplicative` |     |
| ✅ 排除法         | counting    | `knowledge/counting.ts`     | 已新增 `exclusion-method` 页，讲“正难则反 / 补集思维”                                            |     |
| ✅ 标数法         | counting    | `knowledge/counting.ts`     | 已新增 `label-counting` 页（网格路径计数）                                                       |     |
| ✅ 消序法         | counting    | `knowledge/counting.ts`     | 已新增 `order-elimination` 页（排列 → 组合 / 含相同元素）                                        |     |
| ✅ 特殊元素优先法 | counting    | `knowledge/counting.ts`     | 已新增 `special-element-first` 页（先难后易）                                                    |     |
| ✅ 固定参照物法   | counting    | `knowledge/counting.ts`     | 已新增 `fixed-reference` 页（环排消旋转）                                                        |     |
| ✅ 对应思想       | word        | `knowledge/word.ts`         | 已新增 `correspondence` 页（盈亏 / 归一 / 分配）                                                 |     |
| ✅ 整体代换       | word        | `knowledge/word.ts`         | 已新增 `overall-substitution` 页（工程、数列通用）                                               |     |
| ✅ 平方差         | numberCalc  | `knowledge/number-calc.ts`  | 已新增 `difference-of-squares` 页                                                                |     |
| ✅ 比较法         | magicSquare | `knowledge/magic-square.ts` | 已新增 `comparison-method` 页（两线相减、抵消公共）                                              |     |
| ✅ 试填法         | magicSquare | `knowledge/magic-square.ts` | 已新增 `trial-fill` 页（剪枝 + 回退）                                                            |     |

---

## 二、题目缺口

### 2.1 零题目标签（必须补）

~~`乘法分配律` 已补 3 道题（0 → 3）~~ 已补。

新增标签中，以下仍 **0 题** 或知识点未建，列入 §4 待办：

| 标签       | 类别       | 现有题目数 | 说明                                                          |
| :--------- | :--------- | :--------- | :------------------------------------------------------------ |
| 周期问题   | numberCalc | 3          | ✅ 已达阈值；**知识点待建** `knowledge/number-calc.ts`        |
| 完全平方数 | numberCalc | 1          | 缺 2 道；**知识点待建**                                       |
| 相遇追及   | travel     | 3          | ✅ 已达阈值；**知识点待建** `knowledge/travel.ts`（新类别）   |
| 火车过桥   | travel     | 1          | 缺 2 道；**知识点待建**                                       |
| 流水行船   | travel     | 1          | 缺 2 道；**知识点待建**                                       |
| 时间统筹   | planning   | 5          | ✅ 已达阈值；**知识点待建** `knowledge/planning.ts`（新类别） |
| 调运选址   | planning   | 4          | ✅ 已达阈值；**知识点待建**                                   |

### 2.2 稀缺标签（≤ 2 道题，建议补到 ≥ 3 道）

按模块分组，括号内为当前题目数（已反映 §2.3 批量补标签后的状态）。

**counting（排列组合 / 计数）**

- 加乘原理 (2)、加法原理 (2)、乘法原理 (1)
- 容斥原理 (3)、捆绑法 (2)、插空法 (1)、隔板法 (1)
- 消序法 (2)、标数法 (2)、特殊元素优先法 (1)、固定参照物法 (1)

**word（应用题）**

- 假设法 (2)、份数法 (2)、画图法 (2)、线段图法 (2)
- 十字交叉法 (2)、整体代换 (2)

**travel（行程，新类别）**

- 相遇追及 (3)✅、火车过桥 (1)、流水行船 (1)

**numberCalc（计算 / 数论）**

- 整除特征 (2)、同余 (2)、质因数分解 (2)、位值原理 (1)
- 凑整法 (1)、裂项 (2)、平方差 (2)、首尾配对 (?)
- 完全平方数 (1)

**geometry（几何）**

- 勾股定理 (1)、平移法 (2)

**magicSquare（幻方与数阵图）**

- 等差数列法 (2)、试填法 (1)

**planning（统筹规划，新类别）**

- 时间统筹 (5)✅、调运选址 (4)✅

**general（通用）**

- 最不利原则 (1)

### 2.3 未打标签的题目（剩余 3 道）

原 34 道已批量补标签（详见 §5 决策）。剩余 3 道在现有 63 个标签中找不到合适入口，暂留 `tags: []`：

| 模块         | 题目 ID | 现状说明                                                                                 |
| :----------- | :------ | :--------------------------------------------------------------------------------------- |
| counting     | 10035   | 纯 C(n,k) / A(n,k) 公式题，不属于任一"方法/思维"入口；若后续新增 `排列组合公式` 标签再补 |
| numberTheory | 10043   | GCD/LCM 短除模型；1 道题不达阈值，等再积累 ≥2 道后考虑新增 `公约与公倍` 标签             |
| calc         | 10057   | 分数作差比较；与 `magicSquare.比较法` 语义不同，待看是否新增 `numberCalc.比较作差`       |

---

## 三、结构性观察

1. ~~**travel（行程）模块暂无专属方法标签**~~ 已在 §5 决策中新增 `travel` 类别，落地 3 条标签。
2. ~~**numberTheory 模块方法标签偏少**~~ 已补 `周期问题 / 完全平方数` 2 条到 `numberCalc`；`公约与公倍 / 进位制` 等暂不新增，题量不足。
3. **docs/ 与 tags.ts 的对齐**：
   - `docs/几何五大模型.md` 对应 `蝴蝶/鸟头/相似/燕尾/等积变形` —— 已全部有标签与知识点，可继续增题。
   - ~~`docs/排列组合.md` 对应 counting 全集 —— **标签齐全但知识点缺 7 条**~~ 已补齐（见 §1）。
   - ~~`docs/幻方与数阵图.md` 对应 magicSquare 全集 —— **缺 `比较法 / 试填法` 两个知识点**~~ 已补齐。
   - ~~`docs/统筹规划.md` 中的"最优化/运筹"思想目前没有对应方法标签~~ 已在 §5 新增 `planning` 类别，落地 `时间统筹 / 调运选址` 2 条。

---

## 四、建议的下一步

按投入产出排序：

1. ~~**补 2 个高频缺页**：`比较法`、`排除法` 知识点~~（已补）。
2. ~~**补 10 个补齐型知识点**：加法原理、乘法原理、标数法、消序法、特殊元素优先法、固定参照物法、对应思想、整体代换、平方差、试填法~~（已补）。
3. ~~**为 `乘法分配律` 补 3 道题**（0 → 3）~~（已补）。
4. ~~**批量补标签**：清理 §2.3 的 34 道空 `tags` 题~~（已补 31 道；剩 3 道见 §2.3）。
5. ~~**策略决策**：是否扩展 `METHOD_TAGS`~~（已决策，详见 §5）。
6. **新增 7 个知识点页**（对应本轮新增的 7 个方法标签）：
   - `numberCalc`：`periodic-problem`（周期问题）、`perfect-square`（完全平方数）
   - 新建 `knowledge/travel.ts`：`meeting-and-catch-up`（相遇追及）、`train-crossing-bridge`（火车过桥）、`boat-in-current`（流水行船）
   - 新建 `knowledge/planning.ts`：`time-scheduling`（时间统筹）、`routing-and-siting`（调运选址）
7. **补题量未达阈值的标签**：`完全平方数`、`火车过桥`、`流水行船` 各补 2 道至 ≥3 道。

---

## 五、METHOD_TAGS 扩展决策（已落地）

决策时间：批量补标签时同步落地。基线规则不变："每标签 ≥3 道题" 是建议阈值，新增标签可以先建框架、等题量跟上再补知识点。

**新增 7 条标签（56 → 63）**：

| 类别键               | 新增标签     | 覆盖题目                                | 说明                                      |
| :------------------- | :----------- | :-------------------------------------- | :---------------------------------------- |
| `travel`（新类别）   | `相遇追及`   | `10013` `10014` `10019`                 | 行程最核心子题型；同时覆盖环形跑道        |
| `travel`             | `火车过桥`   | `10053`                                 | 1 道，待补                                |
| `travel`             | `流水行船`   | `10054`                                 | 1 道，待补                                |
| `numberCalc`         | `周期问题`   | `10005` `10009` `10046`                 | 彩灯循环 / 算星期 / 余数日期              |
| `numberCalc`         | `完全平方数` | `10047`                                 | 1 道，待补                                |
| `planning`（新类别） | `时间统筹`   | `10069` `10070` `10071` `10072` `10073` | 沏茶 / 排队 / 烙饼 / 过桥 / 双工序        |
| `planning`           | `调运选址`   | `10074` `10075` `10076` `10077`         | 车站选址 / 物资调运 / 管道铺设 / 跟车装卸 |

**显式未采纳**：

- `比例行程`（`10055`）→ 复用 `word.比例法`，保持"比例法"作为跨模块思维方法的单一入口。
- `多次相遇 / 发车间隔 / 环形跑道` 独立标签 → 无现成题目，等有题再加。
- `余数分析` → 与现有 `同余` 语义重叠，不新增。
- `公约与公倍`（`10043`）→ 只 1 道题，暂留 `tags: []`。
- `博弈策略 / 必胜策略`（`10083`）→ 只 1 道题，复用 `不变量`（对称不变性）。
- `排列组合公式`、`比较作差` 等逐题型标签 → 单题暂不开新标签。

**UI 影响**：

- `src/pages/knowledge-index-page.tsx` 和 `src/pages/knowledge-page.tsx` 的 `CATEGORY_LABEL` 已补齐 `travel: "行程"`、`planning: "统筹规划"`，以满足 `Record<KnowledgeCategory, string>` 的穷尽性检查。
- 两个新类别在知识点补齐之前，`CATEGORY_ORDER` 过滤逻辑会自动跳过（`grouped.get(cat)?.length ?? 0 > 0`）。
